<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adeevafatmah.d6">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DoaSetelahMakan"></activity>
        <activity android:name=".DoaSebelumMakan" />
        <activity android:name=".DoaMasukWC" />
        <activity android:name=".DoaKeluarWC" />
        <activity android:name=".DoaDalamPerjalanan" />
        <activity android:name=".DoaBukaPuasa" />
        <activity android:name=".DoaBangunTidur" />
        <activity android:name=".mainSholat" />
        <activity android:name=".mainHalal" />
        <activity android:name=".mainDoa" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>